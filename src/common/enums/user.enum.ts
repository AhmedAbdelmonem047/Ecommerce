
export enum UserGender {
    MALE = "male",
    FEMALE = "female"
}

export enum UserProvider {
    LOCAL = "local",
    GOOGLE = "google"
}

export enum UserRoleEnum {
    USER = "user",
    ADMIN = "admin"
}