
export enum UserGender {
    MALE = "male",
    FEMALE = "female"
}

export enum UserProvider {
    LOCAL = "local",
    GOOGLE = "google"
}

export enum UserRole {
    USER = "user",
    ADMIN = "admin"
}